/** Shared layout classes for dashboard / admin page roots. */
export const dashboardPageMainClass =
  "flex min-h-0 flex-1 flex-col gap-4 p-5 md:p-6"

/** Full-height views (slideshows) that manage their own internal scroll. */
export const dashboardSlideshowMainClass =
  "flex min-h-0 flex-1 flex-col gap-3 overflow-y-auto p-4 md:gap-3 md:overflow-hidden md:p-5"
